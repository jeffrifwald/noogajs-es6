NoogaJS ECMAScript 6 Talk
==

## Setup
1. Install npm
1. `git clone https://github.com/jmcriffey/noogajs-es6`
1. `cd noogajs-es6`
1. Run `npm install`
1. Write es6 in `src/*.js`
1. Run `npm start` to build
1. Open `index.html` and bask in the glory of es6
